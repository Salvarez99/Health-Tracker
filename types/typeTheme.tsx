export type typeTheme = {
  isDark: boolean;
  colors: {
    primary: string;
    secondary: string;
    textColor: string;
    backgroundColor: string;
    headerColor: string;
    chartBackgroundColor: string;
    axisColor: string;
    chartLineColor: string;
    footerColor: string;
    buttonColor: string;
  },
  chartColors : {

  },
  screenColors: {
    
  }
};
