export type typeTheme = {
  isDark: boolean;
  colors: {
    textColor: string;
    backgroundColor: string;
    headerColor: string;
    chartBackgroundColor: string;
    axisColor: string;
    chartLineColor: string;
    footerColor: string;
  };
};
